# Deployment-only overrides. See ../.env.example for the canonical list.
#
# Layer 3 (Docker Compose on one host):
#   docker compose -f infra/docker-compose.yml up --build -d
#   python scripts/seed_regulatory_corpus.py --remote http://localhost:8001 --reset
#
RAG_SERVICE_URL=http://rag-service:8001
DOC_ANALYZER_SERVICE_URL=http://doc-analyzer-service:8002
GUARDRAILS_SERVICE_URL=http://guardrails-service:8003
LANGGRAPH_AGENT_SERVICE_URL=http://langgraph-agent-service:8004

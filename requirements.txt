# =====================================================================
# AI-Native Regulatory Document Auditor - Root requirements
# Single venv covers all FastAPI services for local development.
# Per-service requirements.txt files mirror the relevant subset for
# Docker deployments.
# =====================================================================

# Core web stack
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# Shared schemas (installed in editable mode via the bootstrap script)
# -e ./shared/schemas    # add manually after running `pip install -r requirements.txt`

# RAG / embeddings / vector store
chromadb==0.5.20
sentence-transformers==3.2.1

# Document parsing
pdfplumber==0.11.4

# Agent + LLM
langgraph==0.2.45
langchain==0.3.7
langchain-core==0.3.15
langchain-openai==0.2.5
openai==1.54.3

# Persistence (orchestrator)
sqlalchemy==2.0.36

# Dev / test
pytest==8.3.3
pytest-asyncio==0.24.0

# Dataset import utilities
pandas==2.2.3
openpyxl==3.1.5
